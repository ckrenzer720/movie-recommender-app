{
  "name": "movie-recommender-app",
  "version": "1.0.0",
  "description": "Personalized movie recommendations with trailers, ratings, and favorites.",
  "main": "index.js",
  "scripts": {
    "dev": "live-server --host=127.0.0.1 --open=/",
    "dev:all": "concurrently -n SERVER,WEB -c blue,green \"npm run server\" \"npm run dev\"",
    "start": "live-server --host=127.0.0.1 --open=/",
    "start:all": "concurrently -n SERVER,WEB -c blue,green \"npm run server\" \"npm run start\"",
    "server": "cd server && node index.js",
    "smoke": "cd server && npm run smoke",
    "predeploy": "cd server && npm run predeploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "movies",
    "recommender",
    "trailers",
    "favorites"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2"
  }
}
