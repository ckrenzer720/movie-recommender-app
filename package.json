{
  "name": "movie-recommender-app",
  "version": "1.0.0",
  "description": "Personalized movie recommendations with trailers, ratings, and favorites.",
  "main": "index.js",
  "scripts": {
    "dev": "live-server --host=127.0.0.1 --open=/",
    "start": "live-server --host=127.0.0.1 --open=/",
    "server": "cd server && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "movies",
    "recommender",
    "trailers",
    "favorites"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
